{
  "name": "hiraya-app-starter",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "bunx --bun vite",
    "build": "bunx --bun tsc --noEmit && bunx --bun vite build",
    "test": "bun test",
    "package": "bun run build && hiraya-app package dist"
  },
  "dependencies": {
    "@hiraya/apps-ui": "workspace:*",
    "@hiraya/apps-sdk": "workspace:*"
  },
  "devDependencies": {
    "@hiraya/app-cli": "workspace:*",
    "typescript": "~5.8.3",
    "vite": "^7.0.4"
  }
}
